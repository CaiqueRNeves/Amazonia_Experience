import Profile from './Profile';
import ProfileVisits from './ProfileVisits';

// Exporta todos os componentes do perfil
export {
  Profile,
  ProfileVisits
};

// Exportação padrão da página principal de perfil
export default Profile;